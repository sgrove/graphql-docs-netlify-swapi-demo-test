name: CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Gatsby Publish
    - uses: enriikke/gatsby-gh-pages-action@v2.1.1
      with:
        access-token: abc
        deploy-branch: gh-pages
